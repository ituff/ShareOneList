# ShareOneList 功能路线 TODO

> 2026-08-14 讨论整理。仅规划，不代表已进入开发。

## 0. 统一决策点

- [ ] 确定移动版目标平台优先级（Android / iOS / 仅 Web 响应式）
- [ ] 确定 AI 模型提供商策略（国际模型 / 国内模型 / 可配置）
- [ ] 确定挂载功能目标平台优先级（macOS Finder 先行 / Windows 先行）
- [ ] 确定“比较”功能是只读工具还是含同步执行
- [ ] 评估四个功能之间的依赖关系（AI 索引缓存、比较缓存、挂载缓存可共用）

## 1. 移动版发布（基于 Tauri 2 重写版）

### 技术前提

- [ ] 明确 WinUI 3 分支不做移动迁移，移动版只基于 `tauri-app`
- [ ] 跑通 Tauri 2 iOS / Android 脚手架（生成 `gen/android`、`gen/apple`）
- [ ] 为移动端调整 `tauri.conf.json`（identifier、bundle、移动端配置）
- [ ] 新增移动端 CI workflow（Android SDK/NDK、Xcode、签名）

### 认证与安全

- [ ] 将 OAuth localhost 回调改为 deep link / App 内 WebView 认证
- [ ] 在 Global 和世纪互联 Azure 门户分别注册移动端重定向 URI
- [ ] 验证并启用移动端安全存储（iOS Keychain / Android Keystore），或替换 keyring 插件

### 文件与能力适配

- [ ] 下载改为沙盒目录 + 系统分享 / 保存到相册
- [ ] 上传改为文档选择器 / 分享面板
- [ ] 裁剪或隐藏桌面专属功能（Aria2/IDM/Motrix、关机下载、GitHub 更新器）
- [ ] 更新流程改为应用商店渠道

### 前端适配

- [ ] 响应式布局（侧边栏 → 底部导航）
- [ ] 触摸目标、单指操作、小屏文件列表适配
- [ ] 移动端端到端验证（登录、浏览、下载、预览）

### 备选路线

- [ ] 评估响应式 Web / PWA 作为快速验证方案

## 2. 首页 AI 聊天窗口 + 云端知识库（本地 RAG）

### 产品边界

- [ ] 明确“搜文件”与“基于内容问答”两条能力的优先级
- [ ] 定义知识范围（当前账号 / 指定库 / 所有账号）

### Phase 1：轻量问答

- [ ] 支持选择单个文件 / 文件夹作为问答上下文
- [ ] 抽取文本内容（文本、Markdown 优先）
- [ ] Graph 搜索作为兜底（定位文件、片段）

### Phase 2：本地 RAG

- [ ] 实现 `KnowledgeIndexer`：Graph delta 增量索引 + 索引游标持久化
- [ ] 文档解析：Word / Excel / PPT / PDF / Markdown
- [ ] 分块 + embedding + SQLite 向量索引
- [ ] 实现 `ChatEngine`：检索 Top-K、组装 prompt、流式输出
- [ ] 答案带引用卡片，点击跳转到文件浏览 / 预览
- [ ] 索引进度通过 Tauri 事件推送到前端

### 权限与合规

- [ ] 索引按账号 / 云环境隔离
- [ ] 登出或移除账号时清理对应索引
- [ ] 显示“数据将发送给 XX 模型”的提示与开关
- [ ] 支持排除指定库 / 目录

### 模型抽象

- [ ] 定义 Provider 接口（Embedding 与 Chat 分开）
- [ ] 支持 OpenAI-compatible 国际模型与国内模型切换
- [ ] 设置页新增 API Key 配置

### 未来演进

- [ ] 设计索引 / 检索可替换为服务端实现（兼容移动端）

## 3. 挂载到本地磁盘（WebDAV 中转方案）

### PoC 验证

- [ ] Rust 后端实现 loopback WebDAV 服务
- [ ] 仅实现 `PROPFIND` + `GET` 只读挂载
- [ ] 在真实 macOS Finder 挂载验证：中文名、`#`/`%`、大文件、连接稳定性
- [ ] 验证 Windows 资源管理器挂载体验
- [ ] 根据 PoC 结果决定是否继续 / 是否切换 WinFsp

### 核心设计

- [ ] URL 路径约定 `/{cloudEnv}/{driveId}/{path}`，支持多账号
- [ ] 应用内生成挂载口令，不暴露微软账号密码
- [ ] 方法映射：`GET` / `PUT` / `MKCOL` / `DELETE` / `MOVE` / `COPY` / `LOCK`
- [ ] 元数据 SQLite 缓存 + 内容块 LRU 缓存，防 Graph 限流
- [ ] 服务随应用启停，崩溃 / 退出时清理挂载状态

### 后续阶段

- [ ] 写操作与冲突处理
- [ ] Windows 端评估 WinFsp 路线
- [ ] 挂载设置页（端口、口令、只读开关、自动挂载）

## 4. 文件夹比较 + 同步（Beyond Compare 风格）

### 会话模型

- [ ] 定义可保存的 `CompareSession`（源端点、目标端点、规则、过滤器）
- [ ] 端点支持账号 + 云环境 + OneDrive / SharePoint 库 + 起始文件夹
- [ ] 第一天支持跨账号 / Global↔世纪互联 对比

### Phase 1：只读比较

- [ ] 双栏目录树 + 懒加载（不递归全量拉取）
- [ ] 文件级差异判断：Graph hash → 大小 + 修改时间 → 按需内容校验
- [ ] 状态图例：相同 / 差异 / 仅单侧 / 冲突 / 未比较
- [ ] 过滤规则：文件名、扩展名、文件夹、只看差异
- [ ] 双栏滚动同步、点击跳转到对应项
- [ ] 导出比较报告
- [ ] 打开文件跳转到现有文件浏览器 / 预览

### Phase 2：比较 + 动作

- [ ] 差异列表生成同步计划（复制 / 覆盖 / 删除 / 跳过）
- [ ] dry-run 模式：执行前展示完整影响
- [ ] 删除默认不勾选，二次确认
- [ ] 可保存 / 重开会话
- [ ] 后台预取整棵树 + SQLite 元数据缓存
- [ ] 文本 / Markdown 内容 diff

### Phase 3：进阶

- [ ] Office / PDF / 图片内容比较
- [ ] 双向合并与冲突策略
- [ ] 定时比较 / 增量刷新
- [ ] 跨账号复制优先使用 Graph 服务端 copy，跨云回退流式中转

## 5. 通用技术底座（多功能复用）

- [ ] 统一的 Graph 元数据缓存与限流 / 退避策略
- [ ] 统一的传输 stream 层（下载 / 上传 / 跨账号中转）
- [ ] 统一的 SQLite 本地库（缓存、比较会话、AI 索引、挂载配置）
- [ ] 统一的后台任务与进度事件模型
