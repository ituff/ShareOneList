# 更新日志 / Changelog

每个版本发布前在此文件顶部添加对应章节，CI 会把该章节内容作为 GitHub Release 的更新说明（应用内"检查更新"气泡与通知也会显示这些内容）。

## v2.3.0-beta.1

- 新增"云盘挂载"（设置 → 挂载）：内置本地 WebDAV 网关，把 OneDrive / SharePoint 文档库直接挂载为 Windows 资源管理器盘符或 macOS Finder 卷，无需安装第三方驱动
- 挂载支持多账号、多挂载点：范围可选我的 OneDrive / 站点文档库 / 子目录，Windows 下可指定盘符；网关随应用自启，App 退出后挂载即离线
- Windows WebDAV 诊断与一键修复：自动检测 WebClient 服务、Basic 认证策略与 50 MB 单文件上限并提权修复；无 WebClient 的环境（如 Windows Home）可复制地址与凭据，交给第三方 WebDAV 客户端使用
- 会议录像新增"下载会议脚本"：录像预览页可把 Teams 会议转录一键导出为 .txt 纯文本；转录经 Stream 播放器安全获取，他人共享的会议录像同样支持（首次需先播放视频几秒）

## v2.2.0

v2.2.0 正式版发布，包含测试版（beta.1 – beta.3）的全部功能。

**AI 助手（问 AI）**

- 接入任意 OpenAI 兼容模型供应商：内置 OpenAI / Azure OpenAI / DeepSeek / 阿里百炼 / Moonshot / 智谱 / Ollama 预设，支持连接测试与在线拉取模型列表，可调思考强度
- 回答基于你的云端文件：自动搜索 OneDrive / SharePoint 并注入文档内容（docx / pptx / xlsx / pdf / 纯文本），回答附带可点击引用卡片；未命中时先询问再使用公共知识
- 推理模型思考过程实时展示，自动折叠为"思考 · 耗时"一行，可展开回看
- 聊天历史本地持久化：重启不丢会话，支持历史会话切换、删除与多轮上下文
- AI 记忆：用 #remember / #forget 沉淀持久偏好，设置页可管理，回答中披露注入了哪些记忆
- 首页改为"搜索 + 问 AI"双入口；搜索结果可按来源账户、文件类型、修改日期筛选

**Drive Catalog 目录地图**

- 跨站点、跨文档库的渐进式位置索引（catalog.db + FTS5）：浏览、搜索与 AI 读取持续积累，越用越准
- 新增目录地图（sitemap）页面，查看已积累的目录结构

**配置备份与恢复**

- 一键导出 / 导入配置备份：包含全部设置与账户信息（不含登录凭据），换机重装一步恢复
- 自动备份：指定 OneDrive 同步文件夹后，设置或账户变更自动写入最新备份，由 OneDrive 同步到云端

**下载与更新**

- 更新渠道切换：设置 → 关于 中可在正式版 / 测试版之间切换，测试版可收到预发布推送（含预发布版本号比较）
- 通知中心：右上角铃铛 + 未读角标，集中展示版本更新与任务通知；启动时静默检查更新
- 更新包与文件下载全面支持国内加速镜像自动回退，流式写入磁盘 + 实时进度

**界面与其他**

- 左侧边栏可收起为图标栏；文件列表列头点击排序；长路径面包屑自动折叠
- 全新应用图标；新增日本語 / Deutsch 界面语言；工具页改为卡片式入口
- 世纪互联登录切换至新合规 Client ID
- 修复 AI 回答排版、聊天区域留白与窗口级滚动等问题

## v2.2.0-beta.3

- 新增"备份与恢复"：一键导出/导入配置备份文件（包含全部设置与账户信息，不含登录凭据），换机或重装后一步恢复
- 新增自动备份：指定一个 OneDrive 同步文件夹后，每次设置或账户变更都会自动写入最新配置备份，由 OneDrive 同步到云端
- CI：tag 名带后缀（如 -beta.x）时自动将 Release 标记为预发布版本


## v2.2.0-beta.2

- 更新渠道切换：设置 → 关于 中可在"正式版 / 测试版"之间切换，选择测试版后检查更新与一键更新都会指向最新的预发布版本（含预发布版本号比较，beta.1 → beta.2 → 正式版递进可识别）
- 更新下载全面支持国内加速镜像自动回退（直连 → ghfast.top → gh-proxy.com → github.moeyy.xyz），并改为流式写入磁盘 + 实时进度事件
- 右上角新增通知中心：铃铛按钮 + 未读角标，集中展示版本更新、下载完成、下载失败通知；启动时静默检查更新，发现新版本在右上角弹出气泡，可直接下载
- 重新设计的应用图标：融合 OneDrive 云朵与 SharePoint "S" 元素，修复 macOS/DMG 图标模糊
- macOS 安装包（dmg）内附带 Gatekeeper 修复脚本和中英文首次启动说明
- 文件列表列头（名称/大小/修改日期）可点击切换升序、降序；路径面包屑支持资源管理器式折叠
- 问 AI：首页问题自动带入新会话提交；左侧新增会话栏（新建对话 / 搜索 / 最近列表，可收起）
- 修复 AI 回答渲染行距过大的问题


## v2.2.0-beta.1

- AI 助手（问 AI）：接入任意 OpenAI 兼容模型供应商（内置 OpenAI / Azure OpenAI / DeepSeek / 阿里百炼 / Moonshot / 智谱 / Ollama 预设），支持连接测试、在线拉取模型列表（模糊搜索）、思考强度调节
- AI 回答基于你的云端文件：提问自动搜索 OneDrive / SharePoint 并注入文件内容（支持 docx / pptx / xlsx / pdf / 纯文本解析），回答附带可点击的引用卡片，未命中时先询问再使用公共知识
- 推理模型思考过程：实时展示、自动折叠为"思考 · 耗时"一行、可随时展开回看
- 聊天历史持久化（SQLite）：重启不丢会话，支持历史会话切换、删除、新对话；多轮上下文与引用文件随会话保存
- 首页改造为搜索 + 问 AI 双入口，侧边栏新增「问 AI」「搜索」独立页
- 搜索结果支持按来源账号、文件类型、修改日期筛选
- 聊天输入栏支持多选 M365 账户（记忆上次选择），回答仅基于选中账号
- 修复窗口级滚动条问题，界面随窗口尺寸自适应

## v2.1.1

- 新增右上角通知中心：铃铛按钮 + 未读角标，集中展示版本更新、下载完成、下载失败等通知
- 新增启动静默检查更新：发现新版本时右上角弹出气泡，可直接下载（带进度条）并打开安装器
- 更新安装包下载支持国内加速镜像自动回退（直连 → ghfast.top → gh-proxy.com → github.moeyy.xyz），并改为流式写入磁盘，不再整包占用内存
- 重新设计的应用图标：融合 OneDrive 云朵与 SharePoint "S" 元素，修复 macOS/DMG 图标模糊问题
- macOS 安装包（dmg）内附带 Gatekeeper 修复脚本和中英文首次启动说明
- 文件列表列头（名称/大小/修改日期）可点击切换升序、降序
- 路径面包屑支持资源管理器式折叠：路径过长时前段收进 "…" 下拉，可快速跳转
- 账户支持自定义别名和图标（内置 12 个默认图标）；登录时按 OneDrive driveType 修正个人/组织识别
- 设置页：新增 Teams 录像下载设置（分段并发数 1-16）、GitHub 项目地址、微信公众号二维码
- Windows arm64 补齐绿色版 zip，MSI 文件名去掉 _en-US 后缀，macOS 仅保留 dmg

## v2.1.0

- 账户类型细分：世纪互联版 / 国际版-组织 / 国际版-个人，各类型显示对应的服务入口
- Teams 会议录像：聚合、播放、一键下载；下载受限的录像可用流式提取保存为 MP4
- 跨平台 Tauri 2 重写正式版：Windows（x64 / arm64）与 macOS（Apple Silicon）
- 多账户管理、批量下载任务、断点续传、文件预览、书签等基础能力
