<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SimpleList.Pages.PreviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <!-- Loading indicator -->
        <ProgressRing x:Name="LoadingRing" IsActive="False" HorizontalAlignment="Center" VerticalAlignment="Center" />

        <!-- Image preview -->
        <ScrollViewer x:Name="ImageContainer" Visibility="Collapsed"
                      ZoomMode="Enabled" MinZoomFactor="0.1" MaxZoomFactor="5"
                      HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Image x:Name="PreviewImage" Stretch="Uniform" />
        </ScrollViewer>

        <!-- Markdown preview -->
        <ScrollViewer x:Name="MarkdownContainer" Visibility="Collapsed">
            <controls:MarkdownTextBlock x:Name="MarkdownBlock" Padding="20" Background="Transparent" />
        </ScrollViewer>

        <!-- Media preview -->
        <MediaPlayerElement x:Name="MediaPlayer" Visibility="Collapsed"
                            AreTransportControlsEnabled="True"
                            AutoPlay="True" />

        <!-- Office / Web preview (WebView2) -->
        <WebView2 x:Name="WebViewer" Visibility="Collapsed" />
    </Grid>
</Page>
