<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="SimpleList.Views.CreateDrive"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:SimpleList.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DataContext="{x:Bind vm:CreateDriveViewModel}"
    x:Uid="CreateDrive_Dialog"
    DefaultButton="Primary"
    PrimaryButtonCommand="{Binding CreateDriveCommand}">

    <StackPanel Spacing="12">
        <TextBox x:Uid="CreateDrive_DisplayName" Text="{Binding DisplayName, Mode=TwoWay}" />
        <RadioButtons x:Uid="CreateDrive_CloudType" MaxColumns="2">
            <RadioButton x:Uid="CreateDrive_CloudType_China" IsChecked="{Binding IsChina, Mode=TwoWay}" />
            <RadioButton x:Uid="CreateDrive_CloudType_Global" IsChecked="{Binding IsGlobal, Mode=TwoWay}" />
        </RadioButtons>
    </StackPanel>
</ContentDialog>
